version: '3.7'
services:
  app:
    build:
      args:
        user: aulaSQL
        uid: 1000
      context: ./
      dockerfile: Dockerfile
    image: app-php
    container_name: app
    restart: unless-stopped
    working_dir: /var/www
    volumes:
      - ./:/var/www
    networks:
      - web-net
      - db-net
  
  nginx:
    image: nginx:1.17-alpine
    container_name: nginx
    restart: unless-stopped
    ports:
      - 8000:80
    volumes:
      - ./:/var/www
      - ./docker-compose/nginx:/etc/nginx/conf.d
    networks:
      - web-net

  db:
    image: mysql:5.7
    container_name: db
    restart: unless-stopped
    environment:
      MYSQL_DATABASE: aulaSQL
      MYSQL_ROOT_PASSWORD: 0000
      MYSQL_PASSWORD: 0000
      MYSQL_USER: aulaSQL
    networks:
      - db-net


networks:
  web-net:
    driver: bridge
  db-net:
    driver: bridge